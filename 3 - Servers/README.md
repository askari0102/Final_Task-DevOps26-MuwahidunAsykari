# Servers

## Rebuilding Servers

**1. To verify the _All Servers Rebuild_ requirement, the environment can be destroyed using `terraform destroy` and recreated using the same scripts to achieve an identical state.**
